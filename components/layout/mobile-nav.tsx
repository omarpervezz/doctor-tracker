"use client";import Link from "next/link";import { usePathname } from "next/navigation";import { LayoutDashboard,Stethoscope,Users } from "lucide-react";import { cn } from "@/lib/utils/cn";const links=[{href:"/dashboard",label:"Home",icon:LayoutDashboard},{href:"/doctors",label:"Doctors",icon:Stethoscope},{href:"/patients",label:"Patients",icon:Users}];export function MobileNav(){const path=usePathname();return <nav className="fixed inset-x-0 bottom-0 z-40 grid grid-cols-3 border-t bg-white p-2 lg:hidden">{links.map(({href,label,icon:Icon})=><Link key={href} href={href} className={cn("flex flex-col items-center gap-1 rounded-xl py-2 text-xs text-slate-500",path.startsWith(href)&&"bg-blue-50 text-blue-700")}><Icon size={19}/>{label}</Link>)}</nav>}
